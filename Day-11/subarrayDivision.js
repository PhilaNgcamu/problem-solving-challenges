function birthday(s, d, m) {
  // squares of the bars(s) = array of integers
  // segment (m) = length = Ron's birth month
  // sum of the integers in the array(squares) = Ron's birth day (d)
  console.log(s, "hh", d, "hhh", m);
}
birthday([1, 2, 1, 3, 2], 3, 2);

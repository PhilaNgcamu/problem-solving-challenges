function cutTheSticks(arr) {
  // // determine the shortest stick
  // let shortest = Math.min(...arr);
  // let max = Math.max(...arr);
  // //Declare out array
  // arr.sort((a, b) => a - b);
  // let a = new Array(arr.length + 1).fill([]);
  // a[0] = arr;
  // let i = 0;
  // while (i < max) {
  //   arr[i] = arr[i] - shortest;
  //   a[i + 1].push(arr[i]);
  //   i++;
  // }
  // return a;
  //return array;
}
console.log(cutTheSticks([1, 2, 3]));
